#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>


int main() {
    int n,list[100];
    scanf("%d",&n);
    if(n>100 || n<0)
    {
        return 0;
    }
     for (int i = 0; i < n; i++) {
        scanf("%d", &list[i]);
    }
    for (int i = 0; i < n; i++) {
        printf("%d ", list[i]);
    }
    printf("\n");
    

    
    return 0;
}

